/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Digital-Growth',
  assetPrefix: '/Digital-Growth',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
